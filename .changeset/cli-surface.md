---
"byollm": patch
---

`install` and `uninstall` become `start` and `stop`, `run` loses its url and
says what it is serving when it starts, and `models` folds into `services`.
The old words keep working for now and say what to type next time.
