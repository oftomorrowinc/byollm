---
"byollm": patch
---

The pairing code is highlighted in the Enter-code step, so the one value
somebody has to carry to another window stops looking like the two lines
around it. Off down a pipe and off under `NO_COLOR`, because an escape inside
a code somebody is about to paste is worse than no emphasis at all.
