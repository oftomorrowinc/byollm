---
"byollm": patch
---

A slot whose every mapped service is currently unreachable refuses at enqueue
with `slot-waiting` rather than queueing a job nobody can answer. The one bit
a site learns is whether the slot needs a person or only time — never which
service, whose device, why, or for how long.
