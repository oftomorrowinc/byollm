node_modules/
dist/
build/
.next/
coverage/
*.tsbuildinfo
.env
.env.local
.DS_Store
byollm.config.json
*.log
.git/_stale_locks/
.tsbuild/

# `pnpm pack` output, staged by the release workflow.
dist-tarballs/
*.tgz
.verified

# The same directory at the repository root. The rule above names it inside
# .git, which is where it is not: an ignore that does not match is an ignore
# that reads as coverage.
_stale_locks/
