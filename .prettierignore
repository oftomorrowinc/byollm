dist
coverage
node_modules
.next
# Tool-generated and gitignored. CI never sees these, so checking them
# locally makes `verify` disagree with the build for no reason.
**/.vercel
**/.env*
pnpm-lock.yaml
site/index.html
packages/server/supabase/migrations
# Hand-authored prose, wrapped deliberately. Prettier dedents continued code
# spans and rewrites emphasis markers, which changes how these render.
specs
PLAN.md
README.md
packages/*/README.md
docs
dist-tarballs/
