/**
 * Deliberately empty.
 *
 * Every option here is an option an integrator does not have, and this app is
 * only evidence if it is built the way a real one would be.
 *
 * @type {import('next').NextConfig}
 */
export default {};
